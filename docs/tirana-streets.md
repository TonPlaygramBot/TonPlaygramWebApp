# Tirana Streets

Tirana Streets is a playable **alpha** city sandbox for portrait mobile browsers. It adds a Games card and its own lobby at `/games/tiranastreets/lobby`, with `/games/tiranastreets` as an alias. Kart Royale, Tennis Royal and the existing games are preserved.

## What is playable

- Free roam: explore central Tirana without a timer, walk or sprint, enter parked cars, drive, brake, reverse, exit and recover to the nearest street.
- AI jobs: courier deliveries, races against Ardi, armed rival missions, and one-to-five-star pursuits. Explorer, Street and Veteran difficulty affect mission clocks, AI pace, incoming damage and reputation. Five stars dispatch military SUVs and soldiers.
- Career: nine sequential chapters, repeatable best times, server-awarded reputation and a sports car after three completed chapters. Reputation is a game progression score.
- Online: free two-to-four-player city rooms, six-character room codes, open-room discovery, ready checks, host start, reconnects and host migration. Rivals compete on individual routes; co-op crews share checkpoint progress.
- Living city: 72 server-simulated pedestrians/riders, 20 traffic vehicles with nearby visible drivers, and Arben’s walk-in weapon shop. People use footpaths, modeled sidewalks and signal-controlled zebra crossings, with varied walking speeds and short pauses. Civilians flee nearby gunfire; police and military dismount, chase and fire with line-of-sight checks.
- Arsenal: 33 Ludo firearm/ordnance IDs, finite ammunition, reloading, health/armor, equipment and ammo purchases, holstering, hit effects and sound. Co-op disables friendly fire; rivals enable it. Free-roam defeat respawns the player after four seconds. Street cash and purchased loadouts last for one run; career reputation and unlocks remain durable.
- Mobile controls: camera-relative movement, a steering stick, gas/brake/sprint buttons, enter/exit action, right-side camera drag, minimap and full map. Touch FIRE, RELOAD and ARSENAL accompany the existing movement controls. WASD/arrows move, Shift sprints, E enters/exits, F fires, R reloads, Q opens the arsenal, H holsters and Escape opens the menu. Recover remains in the menu. Finger drag controls horizontal and vertical aim. Hold and drag FIRE with one thumb while moving with the other. The shoulder camera starts nearly level, uses a 58° field of view, and follows behind at 3.8 m. The server bounds aim yaw/pitch, checks target height and line of sight; shots end at the ground when aimed down. Building occlusion remains a conservative horizontal test rather than full mesh ballistics.

## Tirana geography and assets

The checked-in OpenStreetMap extract supplies real streets, building footprints, parks, water and a connected road graph. The district covers Skanderbeg Square, the Pyramid, Rinia Park, the Lana corridor, Blloku and Mother Teresa Square. Facades and landmark exteriors are artistic approximations; inferred heights and fictional jobs are identified in the in-game credits. Arben’s shop has a furnished interior with an opening glass door, collision walls, display cases and seven loaded weapon displays. Other building interiors, the entire metropolitan city and a GTA-scale production remain outside this alpha.

The city reuses Kart Royale’s exact Quaternius PBR GLB buildings and Poly Haven asphalt. There are now 657 fitted detail placements; nearest buildings use full-detail meshes and more distant buildings use LOD meshes, backed by batched textured OSM shells. Cars near the player's garage use the attributed Car Concept model with optimized geometry and materials. Traffic and the military convoy retain lighter Kenney GLBs. The animated human is the same Soldier rig listed by Chess Battle Royal. It is free for games under Adobe Mixamo terms, **not CC0 or open-source content**. Other new assets use CC0 or CC BY 4.0.

The seven textured Ludo weapon sources are included with per-artist credits. Blocked Poly Pizza downloads and unverified Webaverse assets use documented licensed substitutes; this is **33 gameplay slots, not 33 distinct exact meshes**. Several explosive slots share a grenade model/effect. See `webapp/public/assets/tirana-streets/living/ATTRIBUTION.md` and `living-sources.json` for exact mappings, changes and checksums. Civilian/player/dealer/police roles still share the imported human mesh and rig. NPCs vary their proportions and six shared material tints; these are appearance variants, not newly sourced civilian meshes. Human yaw now follows the imported rig’s verified -Z forward direction, including seated drivers and held props.

All shipped models, textures and game audio are local or synthesized; no runtime model CDN or map API is required. The OSM-derived map remains ODbL. `webapp/scripts/build-tirana-map.py` rebuilds it from the extract. The asset scripts embed/resize dependencies, convert CC0 OBJ sources, simplify static GLB geometry, repair legacy glTF buffers and inspect model rigs.

## Runtime structure

`webapp/src/games/tiranastreets/` separates the React lobby/HUD, input, audio, renderer, network transport, and dependency-free simulation/room reducers. `shared/cityLife.mjs` owns combat, inventory, NPC state transitions, pursuit and events; `shared/weapons.mjs` owns the Ludo IDs and arcade balancing. `cityVisuals.ts`, `streetVisuals.ts`, `weaponShop.ts` and `livingVisuals.ts` handle building detail, roads, shop interiors and combat visuals. `shared/streetLayout.mjs` is the authoritative source for shop collision/counter bounds, signal locations and timing. Three.js loads GLB models; repeated PBR details, trees and lamps are instanced and city shells are batched by material. The renderer uses limited local shadows, distance fog and adaptive pixel density. Rendering targets 60 FPS; High Clarity caps pixel density at 2× and Battery Saver disables shadows. The on-screen FPS counter reports current rendering throughput. Device FPS and thermal behavior are not benchmarked yet.

The app transport registers through the existing TPG account/socket flow. `bot/services/tiranaStreets.js` owns room membership, input rate limits, physics, mission clocks, checkpoint order and results. Clients submit bounded controls and monotonic action sequences, never positions, hit targets, ammunition, cash, stars, winners or rewards. Purchases validate distance, wanted status, money and ammo caps. Action retries are deduplicated and the client queues actions in order. Firing rejects stale inputs and enforces reload/fire-rate timing. Actor pools retire inactive clones; NPC counts, dispatches and effect windows are capped. A 30 Hz server loop advances the 60 Hz simulation; clients request snapshots at up to 8 Hz and predict their own movement between snapshots. Stale controls brake after 450 ms. Inactive online seats release after 45 seconds; rooms expire after 30 minutes. Room codes let friends join; this game does not enter the shared paid-stake table queue. It charges no TPG.

`TiranaCareer` stores career progress in MongoDB. Awards use a revision check and completed-chapter deduplication, so retries cannot duplicate reputation. The repository's explicit development memory-store mode remains available. The standalone preview uses the same game and room reducers with authenticated HTTP requests and D1 revisions instead of Socket.IO/MongoDB; preview careers are separate from TPG account careers.

## Validation and release notes

Run:

```sh
node --test test/tiranaCityLife.test.mjs test/tiranaStreets.test.mjs test/tiranaStreetsSocket.test.mjs
npm --prefix webapp run build
```

The focused tests cover geographic connectivity, screen-relative controls, enter/exit/braking, collision, ordered deliveries, AI completion, idempotent career awards, room capacity/ready/host rules, co-op progress, suspended inputs, identity binding, concurrent room creation, GLB dependencies and two real Socket.IO clients including equipment purchase, replay rejection, peer ammunition/star/effect synchronization, reconnect and cleanup. Additional tests cover weapon catalog completeness, shot cadence, reload conservation, wall blocking, shop rules, difficulty, AI/military limits and free-roam respawn.

Before a production release, validate on physical iOS/Android phones and two devices on separate networks, and check MongoDB-backed career writes in the deployment environment. Full bot startup needs its existing native canvas dependency. Simulation rooms currently live in one Node process: horizontal deployment needs sticky room routing or an explicit shared room owner. A server restart ends active app runs while saved careers survive. The standalone preview's D1 rooms persist between worker requests. Online and career mission clocks continue while an in-game menu is open; local AI runs pause.

Deploy the bot and webapp together after review. This pull request does not deploy or merge the production app.

Living-city validation: 18 new GLBs pass the Khronos glTF validator with zero errors. Five inherited warnings remain for tangent generation/non-root skinned meshes. Three.js geometry loading and human animation playback were checked without a GPU. Physical-phone visual, FPS, thermal and cross-network play checks remain necessary. Old persisted preview rooms are upgraded in place when they next advance.


September 2026 street revision: continuous paved sidewalks, raised curb strips and lowered crossing approaches replace scattered pavement samples. Three original GLTF tree variants use tapered branches and individual leaf geometry; lamps, bins, benches, tree grates, drains and bollards are generated with the same local GLTF pipeline. Repeated fixtures are instanced in distance-culled city cells, and opaque shop geometry is merged by material. Signal heads have one active colored lens and traffic uses the same clock as the displayed pedestrian interval. Junction/sign placements are gameplay approximations, not a surveyed record of installed Tirana traffic controls.

Validation for this revision: all 33 focused simulation/Socket.IO tests pass, including walking into/out of the shop, wall and counter enforcement, purchases, signal phases, four camera quadrants and non-destructive upgrades of active rooms. The preview passes TypeScript and its production build. CPU loading of the fixture GLB and construction of the shop/street scene report finite geometry and valid bounds. The portrait browser check reached the app, but this cloud browser has WebGL disabled, so GPU rendering, visual quality and physical-phone FPS remain unverified. The main app release should be reviewed on a WebGL-capable phone before merging.

## Blender and landscape implementation

- Four Blender facade archetypes plus four LODs replace the previous two facade templates on 657 eligible OSM building footprints. Large footprints repeat modules rather than stretching a single bay. Editable `.blend`, generator and CPU model render are included under `webapp/art/tirana-streets` and `webapp/scripts`.
- 2,216 instanced iron railing sections share collision with the server; mapped pedestrian routes, signals and bridge approaches stay open. 205 additional tree placements follow the Lana banks.
- The Lana is recessed to -1.12 m, with a terrain opening, bed, sloped concrete/grass banks, flowing surface normals and solid bridge decks. The missing western reach is inferred from mapped bridge centres. The inaccurate Lana landmark marker was moved to the accessible bank.
- 2K CC0 plaster/ground PBR materials and instanced grass tufts are local. Tufts render within 105 m (45 m in battery mode); repeated street furniture and building detail retain distance culling.
- References: the latest selectable Google Street View capture at the inspected Bajram Curri point was September 2025. Google Earth returned an unavailable-WebGL error in this browser. Google 3D Tiles now have a conditional streaming integration; it remains inactive until an authorized API key/billing project is configured. See tirana-google-3d.md for setup and verification limits. No Google meshes or textures were exported.
- Validation: Blender 4.5.3 LTS exported and CPU-rendered the models; simulation, portrait drag direction, vertical hit/miss, railing clearance/collision, saved-state and socket tests passed. A Three.js CPU scene check validated finite geometry/matrices and confirmed the terrain opening leaves the river visible below street level. Cloud browser WebGL is disabled, so on-device visual smoothness and touch feel remain unverified.


## Continuous streets and portrait controls — September 2026

- Replaced discontinuous pavement strips with a Blender-authored surface triangulated around OSM road/building footprints and river openings: 531 spatial meshes, approximately 84,857 top triangles and 241,613 m² of paving. Curb slopes use the same height function in the simulation. Bridge decks extend beneath the new sidewalks.
- Added 30 bus shelters, 62 bicycle racks, 81 utility cabinets, 59 planters, 70 hydrants, 29 manholes and 68 tactile crossing tiles. These are original, inferred game placements. Solid footprints are shared with the simulation and leave crossing approaches open.
- Added photographed CC0 paver and bark materials (2K/1K color, OpenGL normal, roughness and paving AO), world-scale UVs, outdoor sky reflections, canopy wind and two tree geometry LODs. Vertex-colored canopies share one material. Props/pavements are spatially culled; repeated fixtures are instanced. Draco reduces the street kit from approximately 25 MB to 14 MB; decoding uses two workers and the existing local decoder.
- Added a portrait thumb layout: left stick movement with an automatic run ring; lower-right gear/vehicle/reload/fire, hold-and-drag fire aiming, and separate gas/brake/reverse controls while driving. Pointer ownership prevents one finger from interrupting the other; blur, cancellation and overlays clear held inputs. Camera sensitivity persists locally.
- Authoring: run `node webapp/scripts/export-tirana-street-layout.mjs /tmp/tirana-layout.json`, then Blender 4.5 / bpy with Shapely 2.1 on `webapp/scripts/build-tirana-street-kit.py`, passing that JSON path as the last argument. Source `.blend`, rendered asset inspection and runtime GLB are versioned. The script does not run during normal app builds.
- Verification: `test/tiranaMobileCity.test.mjs` covers touch ownership, screen directions, run hysteresis, fixture clearances and collisions. `webapp/scripts/check-landscape.ts` verifies the final compressed GLB using the bundled official Draco decoder and Three.js: finite geometry/matrices, 370 pavement height samples, empty traffic lanes/river openings and reduced tree LOD geometry. Existing city/simulation/socket tests are retained. GPU rendering and physical-phone touch feel remain unverified in this environment.
- Google 3D remains separately configurable through the existing protected Map Tiles integration. These assets contain no Google imagery or geometry. The result remains an original compact browser game, not an AAA-equivalent or a surveyed digital twin.

The final street GLB stores 4.4 MB of Draco geometry and references 9.2 MB of shared image files through relative glTF image URIs. Image bytes are unchanged; paver diffuse/normal JPEGs are reused directly, and the Blender-exported packed roughness PNG is retained. `materials/street-kit-runtime-sources.json` records their checksums. The compressed editable Blender source uses relative references to the versioned materials; open it within the repository. `scripts/externalize-street-textures.py` reproduces the lossless extraction after Blender export.
