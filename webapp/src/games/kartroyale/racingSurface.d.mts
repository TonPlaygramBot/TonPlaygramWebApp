import type {Track} from './simulation.mjs';
export function surfaceHeight(track:Track,x:number,z:number):number;
export function surfaceColor(track:Track):string;
export function surfaceGrip(track:Track):number;
