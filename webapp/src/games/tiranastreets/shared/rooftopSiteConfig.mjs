// Hotel's official page places its rooftop pool on floor V. The 16-metre
// depiction estimates 3.2 m per storey; no measured height is claimed.
export const ROOFTOP_HEIGHT_ESTIMATES={
 '1315788053':{height:16,basis:'Author estimate: official rooftop pool on floor V × 3.2 m/storey',source:'https://arkahotel.al/arka-rooftop-pool/'}
};
// Isolate this otherwise district-batched shell so the gameplay entrance can
// share bounded material openings with its matching physical interior.
export const DEDICATED_ACCESS_BUILDING_IDS=new Set(['1315788053']);
